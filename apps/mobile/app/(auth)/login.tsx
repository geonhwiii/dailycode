import { LoginScreen } from '@/screens/login';
import { SafeAreaView } from '@dailycode/core';

export default function LoginPage() {
  return (
    <SafeAreaView className='flex-1 bg-white'>
      <LoginScreen />
    </SafeAreaView>
  );
}
